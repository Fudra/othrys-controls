mixin dropzone(parent)
   .dropzone-container
      form(action="upload", class='dropzone', method="post",enctype="multipart/form-data" , id=parent)
         .fallback
            input(type="file", name=parent)